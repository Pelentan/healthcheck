const emailBody = `${parcel.epName} : ${parcel.epId} in error state.  When connecting to ${parcel.epTarget} 
Watcher encountered a fail state.  \n\nEvent Data: \n${parce.eventData} 
\n\nFull Response if received: \n${parcel.response}`;